import React, {useState} from 'react';

function NavBar() {
  const [authStatus, setAuthStatus] = useState(authStatus, setAuthStatus);
  return (
    <div>
      
    </div>
  );
}

export default NavBar;
